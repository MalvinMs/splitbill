EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-publishable-key
# Optional: set this only after deploying the web app to a public HTTPS URL.
# Without it, auth emails use the native app callback instead of localhost.
# EXPO_PUBLIC_AUTH_WEB_REDIRECT_URL=https://your-public-web-app.example/auth/callback
